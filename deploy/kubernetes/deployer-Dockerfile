FROM lachlanevenson/k8s-kubectl:v1.13.2

RUN apk update && apk upgrade && \
    apk add python3 py-pip && \
    pip3 install --upgrade pip && pip3 install invoke pyyaml

RUN mkdir -p ~/.ssh
# Need to add the following for use with circleci:
RUN apk add git openssh